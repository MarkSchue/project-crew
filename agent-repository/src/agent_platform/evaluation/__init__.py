"""Package marker for agent_platform.evaluation (masterplan section 20.3,
plan milestone M8.2).

Evaluation datasets and the deterministic regression runner. Real model
calls are reserved for evaluation suites (masterplan section 28.6); the
runner validates golden outputs today and accepts a pluggable adapter for
real models later.
"""
